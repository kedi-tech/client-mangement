# Shell scripts run inside Alpine containers: CRLF endings would make the
# kernel fail to find the interpreter ("no such file or directory" on a shebang
# that looks fine). Force LF regardless of the contributor's platform.
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
Dockerfile text eol=lf
